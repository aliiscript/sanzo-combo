import { useInput } from 'ink';
export const useSelect = ({ isFocused, state }) => {
    useInput((input, key) => {
        if (key.downArrow) {
            state.focusNextOption();
        }
        if (key.upArrow) {
            state.focusPreviousOption();
        }
        if (input === ' ' || key.return) {
            state.selectFocusedOption();
        }
    }, { isActive: isFocused });
};
//# sourceMappingURL=use-select.js.map
/// <reference types="react" />
import { type Option } from '../types.js';
export type MultiSelectProps = {
    /**
     * When disabled, user input is ignored.
     *
     * @default false
     */
    isDisabled?: boolean;
    /**
     * Number of visible options.
     *
     * @default 5
     */
    visibleOptionCount?: number;
    /**
     * Highlight text in option labels.
     * Useful for filtering options.
     */
    highlightText?: string;
    /**
     * Options.
     */
    options: Option[];
    /**
     * Initially selected option values.
     */
    defaultValue?: string[];
    /**
     * Callback for selecting options.
     */
    onChange?: (value: string[]) => void;
    /**
     * Callback when user presses enter.
     * First argument is an array of selected option values.
     */
    onSubmit?: (value: string[]) => void;
};
export declare function MultiSelect({ isDisabled, visibleOptionCount, highlightText, options, defaultValue, onChange, onSubmit, }: MultiSelectProps): JSX.Element;

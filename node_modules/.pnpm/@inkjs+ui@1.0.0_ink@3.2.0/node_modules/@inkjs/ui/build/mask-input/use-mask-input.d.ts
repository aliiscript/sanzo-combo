import { type MaskInputState } from './use-mask-input-state.js';
export type UseMaskInputProps = {
    /**
     * Listen to user's input. Useful in case there are multiple input components
     * at the same time and input must be "routed" to a specific component.
     *
     * @default true
     */
    isFocused?: boolean;
    /**
     * Text input state.
     */
    state: MaskInputState;
    /**
     * Text to display when `value` is empty.
     *
     * @default ""
     */
    placeholder?: string;
};
export type UseTextInputResult = {
    /**
     * Value to render inside the input.
     */
    inputValue: string;
};
export declare const useMaskInput: ({ isFocused, state, }: UseMaskInputProps) => UseTextInputResult;

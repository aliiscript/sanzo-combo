import React from 'react';
import { Text } from 'ink';
import { useMaskInputState } from './use-mask-input-state.js';
import { useMaskInput } from './use-mask-input.js';
export function MaskInput({ isFocused = true, defaultValue, placeholder = '', mask, onChange, onSubmit, }) {
    const state = useMaskInputState({
        defaultValue,
        mask,
        onChange,
        onSubmit,
    });
    const { inputValue } = useMaskInput({
        isFocused,
        placeholder,
        state,
    });
    return React.createElement(Text, null, inputValue);
}
//# sourceMappingURL=mask-input.js.map
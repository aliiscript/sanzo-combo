import React from 'react';
import { Text } from 'ink';
import { useTextInputState } from './use-text-input-state.js';
import { useTextInput } from './use-text-input.js';
export function TextInput({ isFocused = true, defaultValue, placeholder = '', mask, suggestions, onChange, onSubmit, }) {
    const state = useTextInputState({
        defaultValue,
        suggestions,
        onChange,
        onSubmit,
    });
    const { inputValue } = useTextInput({
        isFocused,
        placeholder,
        mask,
        state,
    });
    return React.createElement(Text, null, inputValue);
}
//# sourceMappingURL=text-input.js.map
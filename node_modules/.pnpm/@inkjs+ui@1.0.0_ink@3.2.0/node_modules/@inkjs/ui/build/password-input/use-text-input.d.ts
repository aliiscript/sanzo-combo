import { type TextInputState } from './use-text-input-state.js';
export type UseTextInputProps = {
    /**
     * Listen to user's input. Useful in case there are multiple input components
     * at the same time and input must be "routed" to a specific component.
     *
     * @default true
     */
    isFocused?: boolean;
    /**
     * Text input state.
     */
    state: TextInputState;
    /**
     * Text to display when `value` is empty.
     *
     * @default ""
     */
    placeholder?: string;
    /**
     * Replace all chars and mask the value. Useful for password inputs.
     */
    mask?: string;
};
export type UseTextInputResult = {
    /**
     * Value to render inside the input.
     */
    inputValue: string;
};
export declare const useTextInput: ({ isFocused, state, mask, placeholder, }: UseTextInputProps) => UseTextInputResult;

import React from 'react';
import { Text } from 'ink';
import { useMultiStyleConfig } from '../theme.js';
export function TableHeaderValue({ children }) {
    const styles = useMultiStyleConfig('Table');
    return React.createElement(Text, { ...styles['headerValue'] }, children);
}
//# sourceMappingURL=table-header-value.js.map
import { type ReactNode } from 'react';
export type TableRowProps = {
    /**
     * Cell.
     */
    children: ReactNode;
    /**
     * Indicates if this row is focused.
     */
    isFocused: boolean;
    /**
     * Indicates if this is a first row.
     */
    isFirst: boolean;
    /**
     * Indicates if this is a last row.
     */
    isLast: boolean;
};
export declare function TableRow({ children, isFocused, isFirst, isLast, }: TableRowProps): JSX.Element;

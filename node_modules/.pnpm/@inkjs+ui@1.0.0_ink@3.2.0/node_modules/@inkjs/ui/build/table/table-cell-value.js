import React from 'react';
import { useMultiStyleConfig } from '../theme.js';
import { Box } from 'ink';
export function TableCell({ children, rowIndex, cellIndex }) {
    const styles = useMultiStyleConfig('Table', { rowIndex, cellIndex });
    return React.createElement(Box, { ...styles['cell'] }, children);
}
//# sourceMappingURL=table-cell-value.js.map
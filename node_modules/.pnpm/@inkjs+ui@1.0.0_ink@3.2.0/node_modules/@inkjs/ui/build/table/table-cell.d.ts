import { type ReactNode } from 'react';
export type TableCellProps = {
    /**
     * Cell.
     */
    children: ReactNode;
    /**
     * Indicates if this row is focused.
     */
    isFocused: boolean;
    /**
     * Indicates if cell is in a first row.
     */
    isFirstRow: boolean;
    /**
     * Indicates if cell is in a last row.
     */
    isLastRow: boolean;
    /**
     * Indicates if this cell is first in a row.
     */
    isFirstCell: boolean;
    /**
     * Indicates if this cell is last in a row.
     */
    isLastCell: boolean;
};
export declare function TableCell({ children, isFocused, isFirstRow, isLastRow, isFirstCell, isLastCell, }: TableCellProps): JSX.Element;

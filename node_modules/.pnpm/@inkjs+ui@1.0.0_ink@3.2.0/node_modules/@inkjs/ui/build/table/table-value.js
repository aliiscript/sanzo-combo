import React from 'react';
import { Text } from 'ink';
import { useMultiStyleConfig } from '../theme.js';
export function TableValue({ children, isFocused, isFirstRow, isLastRow, isFirstCell, isLastCell, }) {
    const styles = useMultiStyleConfig('Table', {
        isFocused,
        isFirstRow,
        isLastRow,
        isFirstCell,
        isLastCell,
    });
    return React.createElement(Text, { ...styles['value'] }, children);
}
//# sourceMappingURL=table-value.js.map
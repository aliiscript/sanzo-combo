import { type ReactNode } from 'react';
export type OrderedListItemProps = {
    /**
     * List item content.
     */
    children: ReactNode;
};
export declare function OrderedListItem({ children }: OrderedListItemProps): JSX.Element;

import { type ReactNode } from 'react';
import { UnorderedListItem } from './unordered-list-item.js';
export type UnorderedListProps = {
    /**
     * List items.
     */
    children: ReactNode;
    /**
     * Marker that's displayed before each list item.
     * If an array is provided, each nested list will use the next marker in that array.
     *
     * @default "─"
     */
    marker?: string | string[];
};
export declare function UnorderedList({ children, marker }: UnorderedListProps): JSX.Element;
export declare namespace UnorderedList {
    var Item: typeof UnorderedListItem;
}

import { ReactNode } from 'react';
export type OptionProps = {
    /**
     * Determines if option is currently focused.
     */
    isFocused: boolean;
    /**
     * Option label.
     */
    children: ReactNode;
};
export declare function Option({ isFocused, children }: OptionProps): JSX.Element;

import React from 'react';
import { Box } from 'ink';
import { useMultiStyleConfig } from '../theme.js';
export function TableRow({ children, isFocused, isFirst, isLast, }) {
    const styles = useMultiStyleConfig('Table', { isFocused, isFirst, isLast });
    return React.createElement(Box, { ...styles['row'] }, children);
}
//# sourceMappingURL=table-row.js.map
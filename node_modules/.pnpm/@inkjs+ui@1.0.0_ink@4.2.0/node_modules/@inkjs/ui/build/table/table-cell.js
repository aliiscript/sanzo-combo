import React from 'react';
import { Box } from 'ink';
import { useMultiStyleConfig } from '../theme.js';
export function TableCell({ children, isFocused, isFirstRow, isLastRow, isFirstCell, isLastCell, }) {
    const styles = useMultiStyleConfig('Table', {
        isFocused,
        isFirstRow,
        isLastRow,
        isFirstCell,
        isLastCell,
    });
    return React.createElement(Box, { ...styles['cell'] }, children);
}
//# sourceMappingURL=table-cell.js.map
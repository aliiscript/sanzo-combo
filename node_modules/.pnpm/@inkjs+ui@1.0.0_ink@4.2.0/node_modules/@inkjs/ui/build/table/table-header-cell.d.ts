import { type ReactNode } from 'react';
export type TableHeaderCellProps = {
    /**
     * Cell.
     */
    children: ReactNode;
};
export declare function TableHeaderCell({ children }: TableHeaderCellProps): JSX.Element;

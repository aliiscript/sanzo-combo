import { type ReactNode } from 'react';
export type TableCellValue = {
    /**
     * Cell.
     */
    children: ReactNode;
    /**
     * Row index.
     */
    rowIndex: number;
    /**
     * Cell index.
     */
    cellIndex: number;
};
export declare function TableCell({ children, rowIndex, cellIndex }: TableCellValue): JSX.Element;

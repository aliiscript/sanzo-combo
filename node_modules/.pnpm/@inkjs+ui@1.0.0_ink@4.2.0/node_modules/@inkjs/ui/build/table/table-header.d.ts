import { type ReactNode } from 'react';
export type TableHeaderProps = {
    /**
     * Columns.
     */
    children: ReactNode;
};
export declare function TableHeader({ children }: TableHeaderProps): JSX.Element;

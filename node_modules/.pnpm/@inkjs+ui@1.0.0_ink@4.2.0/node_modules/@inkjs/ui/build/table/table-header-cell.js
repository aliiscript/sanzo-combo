import React from 'react';
import { Box } from 'ink';
import { useMultiStyleConfig } from '../theme.js';
export function TableHeaderCell({ children }) {
    const styles = useMultiStyleConfig('Table');
    return React.createElement(Box, { ...styles['headerCell'] }, children);
}
//# sourceMappingURL=table-header-cell.js.map
import { type ReactNode } from 'react';
export type TableHeaderValueProps = {
    /**
     * Cell value.
     */
    children: ReactNode;
};
export declare function TableHeaderValue({ children }: TableHeaderValueProps): JSX.Element;

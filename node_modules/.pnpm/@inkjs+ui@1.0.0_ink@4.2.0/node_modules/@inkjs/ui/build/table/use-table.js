import { useInput } from 'ink';
export const useTable = ({ isFocused, state }) => {
    useInput((_input, key) => {
        if (key.downArrow) {
            state.focusNextRow();
        }
        if (key.upArrow) {
            state.focusPreviousRow();
        }
    }, { isActive: isFocused });
};
//# sourceMappingURL=use-table.js.map
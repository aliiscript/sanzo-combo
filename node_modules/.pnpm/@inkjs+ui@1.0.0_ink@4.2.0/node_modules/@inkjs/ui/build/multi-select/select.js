import React from 'react';
import { Box } from 'ink';
import { useMultiStyleConfig } from '../theme.js';
import { SelectOption } from './select-option.js';
import { useSelectState } from './use-select-state.js';
import { useSelect } from './use-select.js';
export function Select({ isFocused = true, defaultLimit, options, defaultValue, onChange, }) {
    const state = useSelectState({ defaultLimit, options, defaultValue, onChange });
    useSelect({ isFocused, state, options });
    const styles = useMultiStyleConfig('Select');
    return (React.createElement(Box, { ...styles['container'] }, state.visibleOptions.map(option => (React.createElement(SelectOption, { key: option.value, isFocused: state.focusedIndex === option.index, isSelected: state.selectedIndex === option.index }, option.label)))));
}
//# sourceMappingURL=select.js.map
import React from 'react';
import { Box, Text } from 'ink';
import figures from 'figures';
import { useMultiStyleConfig } from '../theme.js';
export function SelectOption({ isFocused, isSelected, children, }) {
    const styles = useMultiStyleConfig('Select', { isFocused, isSelected });
    return (React.createElement(Box, { ...styles['optionContainer'] },
        (isFocused || isSelected) && (React.createElement(Text, { ...styles['focusIndicator'] }, isSelected ? figures.tick : figures.pointer)),
        React.createElement(Text, { ...styles['optionLabel'] }, children)));
}
//# sourceMappingURL=select-option.js.map
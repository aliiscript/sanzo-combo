import { type ReactNode } from 'react';
export type UnorderedListItemProps = {
    /**
     * List item content.
     */
    children: ReactNode;
};
export declare function UnorderedListItem({ children }: UnorderedListItemProps): JSX.Element;

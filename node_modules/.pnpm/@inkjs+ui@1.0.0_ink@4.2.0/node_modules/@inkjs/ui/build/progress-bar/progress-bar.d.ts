/// <reference types="react" />
export type ProgressBarProps = {
    /**
     * Progress.
     * Must be between 0 and 100.
     *
     * @default 0
     */
    value: number;
};
export declare function ProgressBar({ value }: ProgressBarProps): JSX.Element;

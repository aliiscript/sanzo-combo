import { type TextProps } from 'ink';
import { type ReactNode } from 'react';
export type BadgeProps = {
    /**
     * Label.
     */
    children: ReactNode;
    /**
     * Color.
     *
     * @default "magenta"
     */
    color?: TextProps['color'];
};
export declare function Badge({ children, color }: BadgeProps): JSX.Element;

const theme = {
    styles: {
        root: ({ color }) => ({
            backgroundColor: color,
        }),
        label: () => ({
            color: 'black',
        }),
    },
};
export default theme;
//# sourceMappingURL=theme.js.map